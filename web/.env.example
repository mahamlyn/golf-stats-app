## Example environment variables for the web app
# Use DATABASE_URL to point to your database. Examples:
# SQLite (default used by the app if DATABASE_URL is not set):
# DATABASE_URL=sqlite:///../db/sqlite/golfstats.db

# SQL Server via ODBC (example - adjust driver & credentials):
# DATABASE_URL=mssql+pyodbc://username:password@SERVER_NAME/DatabaseName?driver=ODBC+Driver+17+for+SQL+Server

# Optional: PORT to control the Flask port
# PORT=5000
